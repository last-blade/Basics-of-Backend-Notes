<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <nav class="w-full fixed top-0 left-0 px-5 py-3 flex justify-between shadow-sm rounded-md">
        <a class="text-2xl font-bold bg-gradient-to-r from-blue-200 via-blue-600 to-indigo-200 inline-block text-transparent bg-clip-text" href="/shop">Scatch</a>
        <% var loggedin = typeof loggedin !== "undefined" ? loggedin: true %>

        <%if(loggedin){ %>
            <div class="flex gap-5">
                <a href="/shop" class="hover:border-b-2 border-blue-500">Shop</a>
                <a href="/cart" class="hover:border-b-2 border-blue-500">Cart</a>
                <a href="/account" class="hover:border-b-2 border-blue-500">My Account</a>
                <a class="text-red-600 font-semibold hover:border-b-2 border-red-500" href="/users/logout">Logout</a>
            </div>
        <% }%>
    </nav>
</body>
</html>