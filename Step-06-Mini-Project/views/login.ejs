<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>

  <div class="w-full h-screen bg-zinc-900 text-white p-10 flex justify-center items-center flex-col">
    <h1 class="text-3xl font-bold mb-3">Login</h1>
    <form action="/login" method="post" class="flex flex-col gap-3">
      <input class="px-3 py-2 rounded-md bg-transparent border-2 border-zinc-800 outline-none" type="text" placeholder="email" name="email">
      <input class="px-3 py-2 rounded-md bg-transparent border-2 border-zinc-800 outline-none" type="password" placeholder="password" name="password">
      <input class="px-4 py-2 rounded-md bg-blue-500 cursor-pointer" type="submit" value="Login">
      <div class="flex gap-3">
        <p>Didn't have an account ?</p>
        <a href="/" class="border-b-2 border-blue-500 text-blue-500">Signup</a>    
    </div>
    <p class="text-red-500"><% if (typeof result !== 'undefined' && result === false) { %>
        Incorrect username or password!
      <% } %>
    </p>  
    </form>
  </div>
  
</body>
</html>